import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn mx-auto loading flex justify-center items-center mt-60 ">loading</button>
        </div>
    );
};

export default Loading;